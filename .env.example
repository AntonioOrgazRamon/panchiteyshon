# NakedCRM Lite - Variables de entorno
# Copiar a .env y ajustar valores

# Servidor
PORT=3000
NODE_ENV=development

# Proveedor de datos: "memory" (fase inicial) | "mongo" (fase final)
DATA_PROVIDER=memory

# MongoDB (solo cuando DATA_PROVIDER=mongo)
# MONGO_URI=mongodb://localhost:27017/nakedcrm

# CORS (orígenes permitidos, separados por coma)
CORS_ORIGIN=http://localhost:4200,http://localhost:5173
