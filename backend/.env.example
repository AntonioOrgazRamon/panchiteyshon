# Copia como .env y rellena valores. No subas .env a ningún repositorio.

# Proveedor de datos: "memory" (sin BD) o "mongo"
DATA_PROVIDER=memory

# Puerto del API
PORT=3000

# Solo si DATA_PROVIDER=mongo: URI de MongoDB
# MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/nombre?retryWrites=true
